services:
  - type: web
    name: kidney-risk-api
    env: docker
    plan: starter
    dockerfilePath: Dockerfile
    autoDeploy: true
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: "8000"
